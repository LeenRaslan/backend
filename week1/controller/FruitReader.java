package reflection.week1.controller;
/*
import lombok.experimental.UtilityClass;
import reflection.week1.model.Fruit;

import java.util.List;
import java.util.function.Function;

import static java.util.stream.Collectors.toList;

@UtilityClass
public class FruitReader {

    public List<Fruit> asList(String filePath) {
        return FileReader.lines(filePath)
                .map(toParticipant())
                .collect(toList());
    }

    private Function<String, Fruit> toParticipant() {
        return line -> Fruit.builder()
                .name(line)
                .build();
    }
}
*/